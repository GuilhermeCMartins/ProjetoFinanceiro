import { styled } from "@/app/stitches.config";

const ContainerFormStyle = styled("div", {
    width: "100%",
    height: "90vh",
    display: "flex",
    justifyContent: "center",
    alignItems: "center"

});

export {
    ContainerFormStyle
}